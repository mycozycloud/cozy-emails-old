.well
  p
    | From: #{model.from()}
    i(style='color: lightgray;') sent #{model.date()}
    br
    if model.get("cc")
      | CC:  #{model.cc()}
  h3 #{model.get("subject")}
  br
  div#mail_content
    if model.get("html")
      !{model.get("html")}
    else
      !{model.text()}
  
.btn-toolbar
  .btn-group
    a.btn.btn-primary
      i.icon-share-alt
      | Answer
    a.btn
      i.icon-share-alt
      | Answer to all
    a.btn
      i.icon-arrow-up
      | Forward
  .btn-group
    a.btn.btn-warning
      i.icon-star
      | Important
    a.btn
      i.icon-ban-circle
      | Spam
    a.btn.btn-danger
      i.icon-remove
      | Delete
